import Styled from 'styled-components'
import { Content } from '../../../components/content/Content'
import { withButton } from '../../../styledMixins/'

export const StyledAddSite = Styled(Content)`
  ${withButton};
`
