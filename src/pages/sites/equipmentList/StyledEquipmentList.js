import Styled from 'styled-components'
import { Content } from '../../../components/content/Content'
import { addIcon } from '../../../styledMixins/addIcon'

export const StyledEquipmentList = Styled(Content)`
  ${addIcon};
`
