import Styled from 'styled-components'
import { StyledTabContent } from '../StyledTabContent'
import { addIcon } from '../../../styledMixins/addIcon'

export const StyledEquipmentList = Styled(StyledTabContent)`
  ${addIcon};
`
