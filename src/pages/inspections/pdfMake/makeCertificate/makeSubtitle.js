import { verticalMargin } from '../constants'

export const makeSubtitle = pageFontSize => ({
  text: 'Australian Playground Standards',
  fontSize: pageFontSize,
  marginBottom: verticalMargin * 4,
  alignment: 'center',
})
