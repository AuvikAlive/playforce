import Styled from 'styled-components'
import { Content } from '../../../components/content/Content'

export const StyledDashboard = Styled(Content)`
  .title {
    margin-bottom: 24px;
  }

  .paper {
    padding: 24px;
  }
`
