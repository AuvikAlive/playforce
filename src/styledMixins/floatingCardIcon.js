export const floatingCardIcon = `
  .card-content {
    position: relative;

    .floating-icon {
      position: absolute;
      right: 16px;
      top: -4px;
      transform: translateY(-50%);
    }

    .floating-icon + .submit-button {
      margin-top: 32px;
    }
  }
`
